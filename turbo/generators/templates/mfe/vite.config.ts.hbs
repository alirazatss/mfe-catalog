import { defineConfig } from "vite-plus";
import { federation } from "@module-federation/vite";

// Implements app-scaffolding: MFE generator requirement (lifecycle contract)
// Implements app-scaffolding: port assignment requirement
// See openspec/changes/mfe-shell-scaffolding/specs/app-scaffolding/spec.md

export default defineConfig({
  optimizeDeps: {
    exclude: [
      "@mfe-runtime/auth",
      "@mfe-runtime/dynamic-loader",
      "@mfe-runtime/events",
      "@mfe-runtime/remote-config",
      "@mfe-runtime/shell-runtime",
    ],
    include: ["react", "react-dom", "react-dom/client"],
  },
  plugins: [
    federation({
      name: "{{ scope }}", // Module Federation scope name
      filename: "remoteEntry.js",
      exposes: {
        "./lifecycle": "./src/bootstrap.ts",
        "./bootstrap": "./src/bootstrap.ts",
        "./App": "./src/bootstrap.ts",
      },
      shared: {
        react: { singleton: true, requiredVersion: "^19" },
        "react-dom": { singleton: true, requiredVersion: "^19" },
      },
    }),
  ],
  server: {
    port: {{ port }},
    origin: "http://localhost:{{ port }}",
  },
  optimizeDeps: {
    include: ["react", "react-dom", "react-dom/client"],
  },
  preview: {
    port: {{ port }},
  },
  build: {
    target: "esnext",
    minify: false,
    cssCodeSplit: false,
  },
});
